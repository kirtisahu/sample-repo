language: 
  - node_js

# Version number
node_js: 
  - 4.2.1 

build:
  ci: 
    - node --version

resources:
  - name: jatin-params
    type: params
    version:
      params:
        boo: foo
  
  - name: jatin-gitRepo
    type: gitRepo
    integration: github
    pointer:
      sourceName: jatindogra/sample_azure
      branch: master

jobs:
  - name: runSh-Job
    type: runSh
    #runtime:
      #architecture: x86_64		
      #os: macOS_10.12
    steps:
      - IN: jatin-params
      - IN: jatin-gitRepo
      - TASK:
        - script: echo "done"
    
 
