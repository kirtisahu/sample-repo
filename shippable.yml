language: 
  - node_js

# Version number
node_js: 
  - 4.2.1 

build:
  ci: 
    - node --version

resources:
  - name: jatin-params
    type: params
    version:
      params:
        boo: foo
  
  - name: jatin-gitrepo
    type: gitRepo
    integration: github
    pointer:
      sourceName: jatindogra/sample_azure
      branch: master

jobs:
  - name: runSh-Job
    type: runSh
    steps:
      - IN: jatin-params
      - IN: jatin-gitRepo
      - TASK:
        - script: echo "done"
    
 
